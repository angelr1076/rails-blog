<%# home page %>
<h1>The Blog</h1>

<h3>All users:</h3>
<div id='users-index'>
    <% @users.each do |user| %>
    <li>
    <%= link_to(user.username, user) %>
    </li>
  <% end %>

  <p>
    <%= link_to('Create User', new_user_path)%>
  </p>

<%#<%= image_tag "gizmo.jpg" %>
